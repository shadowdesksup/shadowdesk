version: '3.8'

services:
  shadow-worker:
    build: .
    container_name: shadow-worker
    volumes:
      # Persistir sessão do WhatsApp entre restarts
      - wwebjs_auth:/app/.wwebjs_auth
    restart: always
    # Recursos para Chromium
    shm_size: '256mb'

volumes:
  wwebjs_auth:
