# Environment Variables
# None strictly required if serviceAccountKey.json is used for Firebase
# and LocalAuth is used for WhatsApp.
# Add any overrides here if needed in future.
NODE_ENV=production
